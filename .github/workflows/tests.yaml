name: Chaos tests

env:
  WEAVIATE_VERSION: preview-prepare-release-v1-24-0-4be295b
  MINIMUM_WEAVIATE_VERSION: 1.15.0 # this is used as the start in the upgrade journey test
on:
  workflow_call:
    inputs:
      lsm_access_strategy:
        required: true
        type: string
    secrets:
      AWS_ACCESS_KEY:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
      DOCKER_USERNAME:
        required: true
      DOCKER_PASSWORD:
        required: true
      GCP_SERVICE_ACCOUNT_BENCHMARKS:
        required: true
      POLARSIGNALS_TOKEN:
        required: true
jobs:
  lint:
    runs-on: ubuntu-latest-8-cores
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
  heave-imports-crashing-1:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-2:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-3:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-4:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-5:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-6:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-7:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-8:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-9:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-10:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-11:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-12:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-13:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-14:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-15:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-16:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-17:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-18:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-19:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-20:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-21:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-22:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-23:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-24:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-25:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-26:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-27:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-28:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-29:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-30:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-31:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-32:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-33:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-34:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-35:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-36:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-37:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-38:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-39:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-40:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-41:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-42:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-43:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-44:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-45:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-46:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-47:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-48:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-49:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
  heave-imports-crashing-50:
    name: Heavy object store imports while crashing
    runs-on: ubuntu-latest-8-cores
    timeout-minutes: 60
    env:
      PERSISTENCE_LSM_ACCESS_STRATEGY: ${{inputs.lsm_access_strategy}}
    steps:
      - uses: actions/checkout@v3
      # - name: Polar Signals Continuous Profiling
      #   uses: polarsignals/gh-actions-ps-profiling@v0.0.1
      #   with:
      #     polarsignals_cloud_token: ${{ secrets.POLARSIGNALS_TOKEN }}
      #     labels: 'job=${{ github.job }};gh_run_id=${{ github.run_id }}'
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}
      - name: Run chaos test
        run: ./import_while_crashing_no_vector.sh
